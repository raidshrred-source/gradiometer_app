name: gradiometer_app
description: Gradiometer Companion with GND save/load
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bluetooth_serial_fork: ^0.3.1 # <-- USE THIS LINE
  fl_chart: ^0.68.0
  path_provider: ^2.1.3
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
